API_URL=http://localhost:3300
# Must match HOST_SECRET in the afridho-api backend. Sent by server.cjs as the
# X-Host-Secret header so the backend accepts only proxied chat requests.
API_SECRET=XXXX

